Place the generated whisper.cpp Emscripten stream runtime here as:
  stream.js

Run ..\vendor-whisper-assets.ps1 on the instructor/developer Windows machine to download the official current build.
The official stream.wasm build embeds the WASM module inside stream.js when WHISPER_WASM_SINGLE_FILE=ON.
